import { Wicked } from "../src/Wicked"; // this will be your custom import
import { expect } from "chai";

describe("Options tests", () => {
  it("checking default options", () => {
    // the single test
    const wicked = new Wicked(); // this will be your class

    // expect(options.detectRetina).to.be.false; // Do I need to explain anything? It's like writing in English!
  });
});
